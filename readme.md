# Go learning project - Password based file encrypter

Simple learning project. The CLI tool will encrypt/deencrypt a given file with a password.

The encryption uses the key derivation function PBKDF2.